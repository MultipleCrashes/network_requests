
# Command to run playbook : ansible-playbook  quark_agent_ansible.yml --extra-vars='ansible_sudo_pass=harish'

---
- name: Verify quark agent proper
  hosts: localhost
  remote_user: harish
  port: 2229
  become: sudo
  tasks:
             

        - name: Grep log file 
          command: cat /var/log/neutron/quark-agent.log | grep ERROR
          register: grep_output
          ignore_errors: true

        - debug:
            #var: "{{grep_output.stdout_lines}}"
            var: grep_output.stdout_lines
            when: grep_output.stdout_lines ! = ''
              #loop:
              # - {'false_positive':'First Message', 'false_negative':'second message'}

          
          



